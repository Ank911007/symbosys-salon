/**
 * Review Controller
 * 
 * Note: The primary review submission endpoint is handled by salon.controller.js (POST /api/salons/:id/reviews)
 * which supports guest users and calculates ratings from approved reviews only.
 * 
 * This file is reserved for future authenticated review operations (e.g., edit, delete own review).
 */

module.exports = {};
